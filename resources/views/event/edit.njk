{% extends "master" %}

{% block content %}

  <div class="row">
    <div class="col-xs-12">
      <div class="content">
          <div class="box box-primary box-solid ">
            <div class="box-header with-border">Edit Event</div>
              <div class="box-body">
                {{ form.open({action: 'Admin/EventController.update', params: {'id': event._id} }) }}
                  {{ csrfField }}
                    {% include 'event/form' %}
                    <div class="box-footer">
                      <button type="submit" class="btn btn-primary pull-right">Save</button>
                    </div>
                {{ form.close() }}
              </div>
          </div>
      </div>
    </div>
  </div>

{% endblock %}
